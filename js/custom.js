
                     // 🔥 let 🔥 

//Declare the letiable_name(fn) with let keyword;
// let fn ; 
// Assign the value with lucky (initialize)
//    fn = "Lucky";
// Redeclare the letiable_name(fn) with let keyword;
// let fn;
// Reassign the value with lucky(same_name) (initialize)

    // fn = "Lucky";
    // console.log(fn); //A-> Lucky
    
// Reassign the value with Happy(different_name) (initialize)

    // fn = "Khamari";
    // console.log(fn); //A-> Khamari  



                                                // 🔥 LET 🔥 

//Declare the variable_name(ln) with let keyword;
let ln ; 
// Assign the value with lucky (initialize)
    ln = "Lucky";

//we cannot  Redeclare the variable_name(ln) with let keyword;
  //let ln;

//  Reassign the value with lucky(same_name) (initialize)
    ln = "Lucky";
    console.log(ln); //A-> Lucky
// Reassign the value with Happy(different_name) (initialize)
    ln = "Khamari";
    console.log(ln); //A-> Khamari  



                                            


    